# B+树
是MySQL的InnoDB 索引默认使用结构

## 特点
* 所有数据都存再叶子节点
* 非叶子节点只存索引键和子节点的指针
* 叶子节点使用的是双向链表，能够快速的进行范围查找
* 所有叶子节点统一再同层
* 同一个片区有很多个中间节点，中间节点也是从小到大排列的
* 每个节点多分支，多元素

## 优势
* 树的层级更低，减少IO的次数
* 支持范围查询，叶子节点使用双向链表链接
* 查询效率稳定，所有数据都在叶子节点

## InnoDB中两种索引对应B+树结构
 * 聚集索引:数据存在叶子节点上
 * 非聚集索引:叶子节点存储的是主键的id，人后再回表查询对应的数据
