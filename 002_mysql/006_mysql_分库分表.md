# 分库分表

## 分表
* 垂直分表：将一个表按照字段拆分，减少单表数据列数，主表和副表在使用一个字段关联
* 水平分表：将数据量拆分，减少单表数据行数，多个表共同作用数据

## 分库
* 垂直分库：按照业务将不同业务的数据拆分，成多个库
* 水平分库：统一个表，拆分成多个一样的数据库，分散再不同的数据库中

## 水平分表查询
* 含有分片键的查询：直接通过分片的键计算到那个表查数据，只操作一张表
* 不含分片键的查询：多个表同时查询，映射表
### 解决方案
* 中间件处理
* 数据厂库的方式 ES
* 建立中间映射表